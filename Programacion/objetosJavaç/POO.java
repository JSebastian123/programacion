package objetosJavaç;

public class POO {
    // Onjetos  y las clases son la gracia Java es un prg oreintado objetos 
    // gracioso jiji 
    // hemos  estado usando clases son como bojetos 
    // ejercicios 

    public class Persona{
        // campos con infos conceptuales 
        // son objetos qe tienen parcidos
        int edad = 20;
        Double estatura = 20.20;
        String DNI = "eqweq123";
        // rutinas y metodos 
        // void main (lo que esta dentro se ejecuta )
        // clases conceptuales que comparteriran un padre 
        
    }
    // public privated 
    // el static te devolveria el valor que queires tipo int 
    public int  obtenerNombreCompleto(){
        return 0;
    }
    public String obtnerNombre(){
        return "Aaa miguel ay miguel";
    }

    // def Scanner es una clase pero se usa como una variable de tipo Scanner
    // para acceder a las variables dentro de las clases que fcreemsos se hace con el 
    // nombredelaclase. y nos dara su variable 
    // instancia, variables creadas pero sin iniciarase, coges la plantailla de las variables sin usar 
    // y les damos valores 
    // la creas en memoria, q es en memoria : la memoria ram hace esto, hace un espacio en memoria que 
    // hace esto 
    //una vez instanciado accedes a el 
    // al instanciar 
    // Precio p 
    // p = new precio
    // p.pone(56.8)
    // clases dentoro de clases
    // solo se puede usar desde la primera clase 
    // herenciar  asi se hace
    // pero es peligroso
    // instanceof es como un equals es compara clases para ver si son el mismo tipo 
}
